# Legacy Runtime Archive

This folder is reserved for legacy runtime files that were part of the old CryBot architecture.

They are kept for reference while the production runtime migrates to:

```txt
src/index.js
```

Do not use files in this archive as Railway or production entrypoints.

Current production entrypoint:

```bash
npm start
```
